import React from "react";

interface IBallotForm {
  candidates: string[];
}

const BallotForm: React.FC<IBallotForm> = ({ candidates }: IBallotForm) => {
  return <></>;
};

export default BallotForm;
